# Tracked files are NOT protected by .git/info/exclude — git ignores ignore-rules for
# tracked paths. This file is graphify's own ignore list and applies regardless of tracking.
# Keep in sync with graphify-secret-gate.sh / graphify-leak-gate.sh.
.env
.env.*
!.env.example
*.pem
*.key
*.p12
*.pfx
*.ppk
id_rsa*
*credential*
*credentials*
*secret*
secrets.yml
secrets.yaml
secrets.json
*.tfstate
*.tfstate.*
*.tfstate.backup
repomix-output.md
.smbcredentials.j2
passwd
vault.yml
vault.yaml
# name-based exclusions (layer 2). Kept in step with the post-run check in graphify-semantic-pass*.sh,
# which refuses to publish a graph whose nodes come from a sensitive-named file. When the two lists
# disagree, a repo gets skipped instead of published — so extend BOTH together.
*password*
*passwd*
*token*
*api-key*
*apikey*
*.conf
*.tfvars
*.tfstate*
.secrets*
*kubeconfig*
*auth.json*
